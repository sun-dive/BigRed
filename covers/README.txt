Drop cover images here, one per listing (e.g. covers/<collectionId>.jpg or .webp).
Reference them from listings.json as "cover": "covers/<filename>".
These are cached hard by the host (immutable), so browsing never refetches them.
